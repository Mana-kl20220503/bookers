<div class = "book-edit">
<h1>Editing Book</h1>

<div class ="error2">
<% if @book.present? && @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

</div>

<%= form_with model: @book do |f| %>

 <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>
  <div class = "action">
    <br><%= f.submit 'Update Book' %>
  </div>




  <tr>
  <br><td class = "a"><%= link_to "Show", book_path(@book.id) %> | <%= link_to "Back", '/books' %></td>
</tr>

<% end %>
</div>
